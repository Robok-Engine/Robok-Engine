<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="dev.trindade.robokide.Robok"
        android:theme="@style/AppTheme">
        <activity
            android:name="dev.trindade.robokide.ui.activities.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="dev.trindade.robokide.ui.activities.DebugActivity"
            android:exported="true" />
            
        <activity
            android:name="dev.trindade.robokide.ui.activities.build.outups.OutputActivity"
            android:exported="true" />    
        
    </application>

</manifest>
